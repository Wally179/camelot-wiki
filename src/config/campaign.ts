/**
 * Conteúdo sensível da wiki (ex.: lista de poderes da Distinção).
 *
 * Enquanto `false`, o site não exibe a lista nem oferece controle para jogadores
 * desbloquearem — só névoa e aviso. Para publicar, altere para `true` e faça deploy.
 */
export const REVEAL_NOVA_TAVOLA_POWERS = false;
